# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd site && npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_HISTOPATOLOGIAORAL_8C7B8 }}'
          channelId: live
          projectId: histopatologiaoral-8c7b8
  deploy_functions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get dependencies
        run: cd functions && npm ci
      - name: Deploy
        uses: jonahsnider/firebase-action@1.0.7
        env:
          PROJECT_ID: "histopatologiaoral-8c7b8"
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HISTOPATOLOGIAORAL_8C7B8 }}
        with:
          args: deploy --only functions
